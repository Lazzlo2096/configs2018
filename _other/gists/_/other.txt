~/.gtkrc-2.0 - это настроичный файл GTK очевидно. В нём меняем темы и иконки, и в openboxe и pcmanfe'e они меняються

openbox зависит от libglib2.0-0 [ https://packages.debian.org/stretch/libglib2.0-0 ] - GLib — это библиотека C-библиотека общего назначения используется в проектах GTK+, GIMP и GNOME.

#Посмотреть зависимости установелнных пакетов:
apt-cache depenence <pkg>
dpkg -l #всё установелнные пактеы в системе

что за папка и файлы в ~/.config/gtk-3.0 ~/.config/gtk-2.0 ? - вроде появились после установки LXappearance.
и что за ~/.config/mimeapps.list ?

 
 /etc/X11/openbox/autostart (или нужно скопировать в домашнюю папку) - автоматически запускает приложения при stratx
 
 
$скрипт чтобы автоматически за'git'чивал (или просто копировал, и потом коимттил) мой (#3.горячие клавиши /etc/X11/openbox/rc.xml), если истекла поставленная дата и если есть изменения в определённых файлах (а то гит ругаеться, аутсайд оф репосит).
Может понадобиться для запоминания конфигов и т.п. при аварийной растройстве системы.
 
 
/etc/X11/openbox/ - это ярлык на /etc/xdg/openbox/

$создать либу для fm для ВСЕЕХ настроек, собрать их отовсюду, зафоркать и мерджить, и написать свой fm


$gmrun, не работают pgDonw и так далее калвиши для переменешия по таб списку
почему список выводиться по табу, хочу чтоб выводился по вводу
и чтоб можно было контролить его размер
и чтоб уже указывал на вариант
и чтоб gmrun закрывался, если потерял фокус
